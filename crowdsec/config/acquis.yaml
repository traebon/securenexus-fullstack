# Dummy acquisition to allow LAPI-only mode
# CrowdSec requires at least one datasource, using journalctl as a no-op source
---
source: journalctl
journalctl_filter:
  - "_SYSTEMD_UNIT=dummy.service"
labels:
  type: syslog
